binwidth=100000000.0
data='queue_time.dat'

set style histogram clustered gap 1
set style fill solid border -1
set boxwidth 0.5*binwidth

bin(x, width)=width*floor(x/width)
plot data using (bin($1,binwidth)):(1.0) smooth freq with boxes
pause 1000
